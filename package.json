{
  "name": "vehicle-maintenance-tracking",
  "version": "0.0.0",
  "private": true,
  "description": "Vehicle Maintenance Tracking System (MTS) \u2014 RFP-012",
  "engines": {
    "node": ">=24.11.0",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:api": "pnpm --filter api dev",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "check": "pnpm format:check && pnpm lint && pnpm build && pnpm test",
    "migration:generate": "pnpm --filter api migration:generate",
    "migration:run": "pnpm --filter api migration:run",
    "migration:revert": "pnpm --filter api migration:revert",
    "seed": "pnpm --filter api seed",
    "seed:prod": "pnpm --filter api seed:prod",
    "migration:run:prod": "pnpm --filter api migration:run:prod",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "db:logs": "docker compose logs -f db",
    "up": "docker compose up -d",
    "down": "docker compose down"
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
